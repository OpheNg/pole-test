<!DOCTYPE html>
<html>
  <head>
    <title>Click & Pole</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script src="https://kit.fontawesome.com/8110e6fe91.js" crossorigin="anonymous"></script>
    <%# <link href="https://api.mapbox.com/mapbox-gl-js/v2.10.0/mapbox-gl.css" rel="stylesheet"> %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <%#= render "shared/navbar" unless params[:action] == 'home' %>
  <%= render "shared/flashes" %>
  <%= render "shared/sidebar" unless params[:action] == 'home' || params[:action] == 'dashboard' %>

    <%= yield %>
  </body>
</html>
